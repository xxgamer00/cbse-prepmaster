{
  "name": "cbse-prepmaster",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm run build",
    "ionic:build": "cd client && npm run build",
    "ionic:serve": "cd client && npm start",
    "resources": "cd client && npm run resources",
    "android": "cd client && npm run android",
    "android:build": "cd client && npm run android:build",
    "android:live": "cd client && npm run android:live",
    "sync": "cd client && npm run sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.1",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.1",
    "@ionic/react": "^7.0.0",
    "@ionic/react-router": "^7.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.19.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.1",
    "@ionic/cli": "^7.1.1",
    "cordova-res": "^0.15.4",
    "native-run": "^1.7.3"
  }
}
